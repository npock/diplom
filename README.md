📦 Установка и запуск
Проект полностью контейнеризирован и готов к запуску в любой среде.
1. Подготовка окружения
Клонируйте репозиторий и создайте файл .env в корне проекта:
env
MONGO_ROOT_USER=admin
MONGO_ROOT_PASSWORD=your_password
MONGO_DB_NAME=questionsdb
PORT=3005
Используйте код с осторожностью.

2. Запуск через Docker
bash
docker compose up -d --build
Используйте код с осторожностью.

После запуска:
Приложение будет доступно по адресу: http://localhost:80 (или IP сервера).
API эндпоинты доступны по адресу: http://localhost:80/api/v1/.
📐 Архитектура REST API
В проекте реализована ресурсная модель API. Примеры эндпоинтов:
GET /api/v1/stuff — получение списка всех товаров.
POST /api/v1/reviews/filter — получение отзывов к конкретному товару.
POST /api/v1/reviews — создание нового отзыва (защищено isAuth).
🐳 Docker Инфраструктура
Проект использует два связанных контейнера:
server: Node.js среда, в которой работает Express-сервер и отдается статика фронтенда.
mongodb: Официальный образ базы данных с выделенным томом (Volume) для сохранения данных при перезагрузке.
👤 Автор
Илья — https://github.com/npock
